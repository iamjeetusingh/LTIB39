<#
    Task
        - ask user for a file name (with extension) & search 
          it in all the drives (c, d, e...)
        - after finding the file, list the location of the 
          file (including reduntent files)

          Hints:
          - get-childitem
            - recurse
            - filter
            - if-else
            - foreach loop
            - read-host
            - write-host
            - |
#>